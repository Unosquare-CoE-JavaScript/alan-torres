// Destructuring is a simplified way to define variables and taking them outside of an array or object

// Arrays
// Variable declarations:
const [first, second] = [true, false];
let [first, second] = [true, false];

// Assigment:
[first, second] = [true, false];


// Objects
// Variable declarations:
const { first, second } = { first: 0, second: 1 };
let { first, second } = { first: 0, second: 1 };

// Assigment:
// { first, second } = { first: 0, second: 1 };
