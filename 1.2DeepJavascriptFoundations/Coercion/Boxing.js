// Boxing: A form of implicity coercion, like accesing properties on primitive values. Ex: "string".length

// Coercion: primitive to object
if (studentNameElem.value.length > 50) {
    console.log("Student's name too long.");
}

// Note: primitives doesn't have properties
