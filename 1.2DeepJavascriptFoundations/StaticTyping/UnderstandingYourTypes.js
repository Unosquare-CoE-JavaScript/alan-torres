// Javascript has a (dynamic) type system, which uses various forms of coercion for value type conversion,
// including equality comparisons.

// The prevailing response seems to be: avoid as much of this system as possible, an use === to "protect"
// from needing to worry about types.
